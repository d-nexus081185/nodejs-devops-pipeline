{
  "name": "nodejs-devops-app",
  "version": "1.0.0",
  "description": "Sample Node.js app for DevOps pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prometheus-client": "^1.0.0"
  }
}
